# varpy
Herramienta de escritorio (Windows) para trazar líneas y evaluar offside desde una foto única, con calibración por homografía, ejes X/Y (override) y Z (punto de fuga), guías para hombro, zoom/pan, tolerancia configurable, Hough para líneas y autodetección simple de jugadores. Exporta a PNG + JSON.
